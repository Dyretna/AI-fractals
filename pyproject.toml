[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-fractals"
version = "0.1.0"
description = """
AI-fractal generator, based on:
-------------------------------
'AI-Enhanced Fractal Geometry: Merging Machine Learning with Traditional Fractal Mathematics'

Author: Douglas C. Youvan
mail: doug@youvan.com
"""
readme = "Readme.md"
requires-python = ">=3.10"
license = {file = "LICENSE"}
authors = [
    {name = "Jesper Anteryd", email = "jesper.anteryd@proton.me"},
]
keywords = ["ai", "fractals"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Programming Language :: Python :: 3.10",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Scientific/Engineering :: Mathematics",
]
dependencies = [
    "numpy>=2.2.0",
    "opencv-python>=4.13.0",
    "matplotlib>=3.10.0",
    "pillow>=12.1.0",
    "tensorflow>=2.20.0",
    "keras>=3.12.0",
    "python-dotenv>=1.2.0",
    "requests>=2.32.0",
]

[project.optional-dependencies]
dev = [
    "ipykernel>=7.2.0",
    "jupyter>=1.0.0",
    "pre-commit>=4.6.0",
]

[project.urls]
Repository = "https://github.com/Dyretna/AI-fractals"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
